---
- name: Deploy demo application
  hosts: local
  become: yes

  tasks:
    - name: Install Python and pip
      apt:
        name:
          - python3
          - python3-pip
        state: present
        update_cache: yes

    - name: Ensure application directory exists
      file:
        path: /opt/myapp
        state: directory

    - name: Upload application file
      copy:
        src: example-app/app.py
        dest: /opt/myapp/app.py

